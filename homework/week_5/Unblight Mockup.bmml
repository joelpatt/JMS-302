def ask question
    while true
        puts question
        reply = gets.chomp.downcase
        
    if reply == 'yes' 
        return true
    end
    if reply == 'no'
        return false
    end
page 1

page 9 page 13

(from on page 80)

# If we got this far, then we're going to loop # and ask the question again.
puts 'Please answer "yes" or "no".'

end end

likes_it = ask 'Do you like eating tacos?' puts likes_it 