file = "restaurants.txt"
unless File.exists? file
    puts "#{file} doesn't exist"
    exit
end

restaurants = array.new # []
File.open(file, "r"). each_line do |line|
    words = line.chomp
puts " Would you like to add to the list? "
File.update(file, "a"). = "restaurant.txt"

puts " Type restaurant names. When complete type done."
    while true
        restaurants = gets.chomp
        if restaurants == "done"
        break
        restaurant.push restaurant
end
            
rand (restaurant.size)
random_restaurant = rand (restaurants.size)
puts " Go to restaurant #{restuarant}"
    restaurant [random_restaurant_index]


